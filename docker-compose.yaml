services:
  pgbackup:
    build:
      context: .
      args:
        - USER_DOCKER=pgbackup
        - UID_DOCKER=10000
    container_name: pgbackup
    restart: always
    environment:
      USER_LOGIN: admin
      USER_PASSWORD: admin
      JWT_SECRET: very_secret_string
    volumes:
      - ./dumps:/app/dumps:rw
      - ./data:/app/data:rw
    ports:
    - 8080:8080
  